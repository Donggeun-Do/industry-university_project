<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oc.api_server.Repository.Review1Repository">
    <select id="getList" resultType="SimpleReview1">
        SELECT r.id, r.title, r.preview as simple_main, is_image FROM review1 r Order by id DESC LIMIT 10
    </select>

    <select id="getListContinue" resultType="SimpleReview1">
        <![CDATA[
        SELECT r.id, r.title, r.preview as simple_main, is_image FROM review1 r WHERE r.id <  #{last} Order by id DESC LIMIT 10
        ]]>
    </select>

    <insert id="SaveReview1" parameterType="Review1">
        insert into review1
        values
            (
                NEXT VALUE FOR review1_id,

                     #{title},
                     #{main},

                     #{preview},
                     #{is_image},

                     #{address1},
                     #{address2},
                     #{x},
                     #{y},

                     #{guarantee},
                     #{price},
                     #{money},
                     #{management},

                     #{people},
                     #{sum_r},

                     #{review_owner},
                     #{total},

                     now(),
                     now()

            )
    </insert>
</mapper>